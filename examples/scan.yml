# Copy to .github/workflows/sensitive-scan.yml in the repository to scan.
# Start with reports and assign a reviewer; findings are possible sensitive data.
# This published v1.0.0 example checks text patterns. For common field extraction
# and GC boolean flags, select a release containing those later changes.
# For production, pin the action to the full commit SHA you reviewed.
# fail-on sets check failure; repository rules separately control merge gating.
name: Sensitivity scan
on:
  pull_request:
  push:
    branches: [main]
  workflow_dispatch:
  schedule:
    - cron: '23 5 * * 1'
permissions:
  contents: read
jobs:
  scan:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 0
          persist-credentials: false
          ref: ${{ github.event.pull_request.head.sha || github.sha }}
      - name: Scan
        id: sensitive
        uses: KingBain/sensitivity-scan@v1.0.0
        with:
          profile: code
          fail-on: NONE
      - name: Preserve reports
        if: ${{ always() && steps.sensitive.outputs.report-directory != '' }}
        uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: sensitive-scan-results
          path: ${{ steps.sensitive.outputs.report-directory }}
          retention-days: 7
          if-no-files-found: error
